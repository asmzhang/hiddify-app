# 后续路线图（2026-09-14 收官时点）

> 前置状态：分层收敛完成（core→features 逆依赖 47→0，提交 42c99230，本地 ahead 35 未推送）；
> analyze 0 error / 251 issues；release build 通过；UI 审计 78%（P0 已清）。

---

## 1. 分析器债务清零（251 issues → 0）

先看构成再动手——**真实手写债务只有 ~110 条**：

| 块 | 数量 | 处理方式 |
|---|---|---|
| `lib/hiddifycore/generated/**/*.pb*.dart`（protobuf 生成） | ~90 | 在 `analysis_options.yaml` 加 `exclude`，一改清零，不碰生成代码 |
| `lib/features/profile/details/json_editor.dart`（第三方移植代码，47 条） | 47 | 单独评估：优先同样 exclude + 文件头注明来源版本；若参与维护再手修 |
| 手写代码 | ~114 | 分批手修，见下 |

手写部分按"高价值→纯风格"排序：

1. **真问题批（~30 条，含全部 24 条 warning）**：dead_code 3、unused_import/unused_element、
   `parameter_assignments`（windows_protocol.dart 深链处理，有逻辑风险）、
   `unreachable_switch_case`（hiddify_core_service.dart:592）、`avoid_print` 3（lib 内禁止 print）。
2. **deprecated 批（11+5）**：`deprecated_member_use` 逐个看 Flutter/插件新 API 替换；
   `deprecated_member_use_from_same_package` 5 条是自家旧接口，顺手收敛。
3. **机械批（~70）**：`prefer_constructors_over_static_methods` 71（多数在生成文件，剩余手写不多）、
   `unnecessary_ignore` 33、`always_use_package_imports` 23、`directives_ordering` 8 等——
   大多可安全批量改，改完 build + 真机冒烟。

每批：analyze 对比 → release build →（动了启动/路由路径时）真机截图。

## 2. 真机回归验证

分层数刀改下来只在 analyze/build 层面验证过，**还没跑过完整真机回归**：

- 双击 `build\windows\x64\runner\Release\Hiddify.exe`
- 核对：启动 → 抽屉头状态行 → 节点页 → 设置各页（入站/路由/覆写）→ per-app 代理页 →
  深链导入（SSRF 确认弹窗）→ 二维码分享/扫码 → 退出弹窗
- 重点关注本轮动过的文件牵连面：db（profile 列表读写）、mixedPort（本地代理/LAN 共享）、
  per-app 模式枚举（偏好持久化读旧值）

## 3. 推送

本地 ahead 35。等真机回归通过 + 你确认时机后一次推送（`git push origin my`）。

## 4. UI 审计遗留（P1/P2）

见 `docs/audit/ui-audit-2026-09-14.md`：P0 已清零；P1/P2 按审计报告里的清单逐项过，
原则不变——以 NekoBox 源码为准绳，改前先核对原版行为，避免上次的"chip 误判"复发。

## 5. 可选收尾

- 死代码二次扫描（tear-off 调用要用符号名搜索，别用调用式 grep）
- `tool/` 下校验脚本整理成统一入口
- 分层规则加进 CI/analyze 自检脚本（逆依赖计数脚本已在 layering.md 第 4 节）
